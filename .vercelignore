# Vercel CLI uses this file (when present) instead of .gitignore to decide
# what to upload. We exclude dev/build artifacts but DELIBERATELY do NOT
# exclude assets/video/*.mp4 — those are gitignored but must still ship to
# the deployment so the Watch tabs play.
node_modules
.git
.vercel
.DS_Store
scripts
*.log
out
