# AI Encyclopedia — X Launch Kit

Assets and a thread skeleton for the launch post. (This file is for you, Costin —
it ships in the repo but isn't linked from the site.)

## Assets checklist

- [ ] `AIE-Promo` 16:9 MP4 (Remotion render — `remotion-demo` project, comp id `AIE-Promo`)
- [ ] `AIE-Promo-Vertical` 9:16 MP4 (same, comp id `AIE-Promo-Vertical`)
- [ ] 4–6 screen-recorded GIFs (8–12s each, see shot list below)
- [ ] OG image renders correctly when the URL is pasted into X (check before posting)

## GIF shot list (these out-travel the video — record at 2× browser zoom)

1. **Attention inspector** — hover "it" → mass lands on "ball"; drag temperature. (`/chapters/03-attention.html`)
2. **XOR playground** — hit TRAIN, watch the decision boundary bend around the circles. (`/ml/07-neural-networks.html`)
3. **Reverse diffusion** — SAMPLE FROM NOISE, ring of clusters re-emerges. (`/chapters/10-diffusion.html`)
4. **Python in the page** — edit the learning rate in a pycell, RUN, loss diverges. (`/ml/02-linear-regression.html`)
5. **The Forge** — drag compute budget, dossier numbers cascade. (`/chapters/capstone.html`)
6. **Gym share card** — finish a deck, download the result card. (`/gym/`)

## Thread skeleton

**Post 1 (with 16:9 video):**
I built a free, interactive AI encyclopedia.

4 volumes: ML from first principles → the complete LLM stack → prompting → agent engineering.
32 chapters · 70+ live instruments · real Python in your browser · a Gym to drill it all.

No paywall. No signup. Open source.

🔗 llm-manual.vercel.app

**Post 2 (attention GIF):**
Every concept is playable. This is real attention — hover a token, watch the
pronoun resolve. Lower the softmax temperature and see it sharpen into a lookup.

**Post 3 (XOR GIF):**
Volume I teaches ML from zero. You don't read about neural networks — you train
one, in the page, and watch the decision boundary learn XOR.

**Post 4 (pycell GIF):**
Real NumPy runs in your browser (WASM). Break the learning rate on purpose.
Watch gradient descent explode. That's how it sticks.

**Post 5 (Forge GIF):**
The capstone: design a frontier model. Compute budget → Chinchilla split →
training cost → serving throughput. Every number traces to an equation you met
in the manual.

**Post 6 (Gym card):**
Then prove it in the Gym — drills, numeric problems, Python katas with hidden
tests. Graded in your browser. Post your card.

**Post 7 (the kicker):**
The site runs on a topic-agnostic engine I'm open-sourcing as a Claude Code
skill: ask for "an interactive field manual about espresso" and you get one,
same craft. github.com/Emposes/field-manual-engine

**Post 8 (the thesis):**
Built with Claude Code (Fable 5). The four new volumes — 21 chapters, ~45
instruments, the Gym, the skill, this video — were produced by a 27-agent
fleet in one session. This is what one person can ship now.

## Timing notes

- Post when US East is awake (16:00–18:00 Romania time).
- Pin the thread; reply to early comments fast — the first hour decides reach.
- Quote-tweet yourself next day with the "llms.txt" angle for the AI-tools crowd.
