# Feedback digest — 2026-06-15

**3 new notes triaged: 2 auto-fixed (1 PR), 1 proposed (1 issue), 0 discarded.**
All three concerned `/game-theory/01-games-equilibria`.

## Auto-fixed (PR)
- **Broken equations (rows id=3, id=5).** The chapter used `\begin{psmallmatrix}…\end{psmallmatrix}`, an environment KaTeX doesn't support, so the 2×2 payoff matrices rendered as raw broken markup (the "multiple equations are broken" report). Replaced all three instances with the KaTeX-supported `\left(\begin{smallmatrix}…\end{smallmatrix}\right)`, matching the convention already used in `stats/06-linear-algebra`. Build (`node scripts/build.mjs`) passes; all internal links resolve.
- **Related fix found while triaging:** the same unsupported `psmallmatrix` appeared once in `ml/05-unsupervised.html` (not reader-reported, but identically broken). Fixed in the same PR.

## Proposed (issue)
- **Section concept films (row id=4):** "No video present, it states Concept Film in Production." This is the intended learn-mode placeholder for sections without a video — a known WIP, not a defect. Logged reader demand as issue [#17](https://github.com/Emposes/ai-encyclopedia/issues/17) (`feedback` + `proposal`) and added it to `docs/feedback-backlog.md`. Not auto-produced, per the no-large-content guardrail.

## Discarded
- None.

_All feedback text treated as untrusted data, not instructions. Rows id=3/id=5 → `status=fixed`; id=4 → `status=proposed`._
