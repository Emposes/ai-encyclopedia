.vercel
.DS_Store
node_modules
# Concept videos are large (~hundreds of MB). They are deployed to Vercel
# via the CLI (see .vercelignore, which does NOT exclude them) but kept out
# of git to keep the repo lean. Regenerate with remotion-demo/scripts.
assets/video/*.mp4
